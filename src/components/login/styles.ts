import { css } from "@emotion/core";

export const errorBox = (theme) => css`
    color: ${theme.errorText};
`;

export const centerLink = (theme) => css`
    text-align: center;
    margin-bottom: 10px;
`;
